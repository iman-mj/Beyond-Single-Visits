# Environment & Hardware Requirements

This model was trained and evaluated using the following hardware configuration:

* **GPU:** NVIDIA GeForce RTX 4090 (24GB VRAM)

### Dependencies
Please ensure your environment meets the necessary VRAM requirements (at least 24GB for full training/inference configurations) to avoid Out-Of-Memory (OOM) errors.

To install the required packages, run:
```bash
pip install -r requirements.txt
